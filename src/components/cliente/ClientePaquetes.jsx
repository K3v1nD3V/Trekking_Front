import React from 'react';

const ClientePaquetes = () => (
  <section>
    <h2 className="detalle-title">Paquetes Disponibles</h2>
    <div className="placeholder">Aquí se mostrarán los paquetes disponibles.</div>
  </section>
);

export default ClientePaquetes;